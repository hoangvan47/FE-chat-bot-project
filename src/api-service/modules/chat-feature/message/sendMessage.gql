mutation SendMessage($content: String!, $threadId: ID) {
  sendMessage(content: $content, threadId: $threadId) {
    success
    message
    threadId
    aiMessage
    usage {
      promptTokens
      completionTokens
      totalTokens
    }
  }
}
